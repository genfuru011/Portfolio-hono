{
  "name": "portfolio-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "build:content": "node scripts/build-posts.mjs",
    "dev": "pnpm run build:content && wrangler dev",
    "deploy": "pnpm run build:content && wrangler deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.8.12",
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.38.0"
  }
}
